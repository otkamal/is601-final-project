# Version control
.git
.gitignore

# Python virtual environments (never belong in the image; the build
# installs dependencies itself via requirements.txt)
.venv
venv
env

# Python bytecode / caches
__pycache__
*.pyc
*.pyo
.pytest_cache
.mypy_cache
.ruff_cache

# Test / coverage output
.coverage
htmlcov
test-results

# Editor / OS cruft
.vscode
.idea
.DS_Store

# Docker itself
Dockerfile
docker-compose.yml
.dockerignore

# CI config (not needed inside the image)
.github
